import 'package:flutter/material.dart';

class TabletFooter extends StatelessWidget {
  const TabletFooter({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
